{
  "name": "audioglyph",
  "version": "0.1.0",
  "private": true,
  "description": "Audioglyph — Vue TS frontend + FastAPI backend monorepo",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -k -n web,api -c blue,green \"pnpm --filter web dev\" \"pnpm run dev:api\"",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "cd backend && uv run uvicorn app.main:app --reload --port 8000",
    "api:install": "cd backend && uv sync",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
